# mongsil-bot

이터널 리턴 전적을 조회하는 디스코드 봇 몽실봇. 닉네임만으로 랭크, 전적, 플레이타임을 디스코드 안에서 바로 확인한다.

## 기능

- `/랭크` — 티어, RP, 순위와 상위 백분율, 승률, 평균 순위, 모스트 캐릭터
- `/랭킹` — 상위 100명 랭킹, 버튼 페이지네이션
- `/이터컷` — 이터니티와 데미갓 RP 컷
- `/플탐` — 최근 7일 플레이타임 통계
- `/시즌` — 현재 시즌 정보
- `/동접` — 현재 동시 접속자 수와 24시간 통계
- `/강아지`, `/고양이` — 무작위 동물 사진
- `/설정` — 서버별 이모지 확대 기능 켜고 끄기
- `/정보` — 봇 정보
- 커스텀 이모지만 있는 메시지를 웹훅으로 확대해 다시 보낸다

## 동작 방식

bser Open API로 전적을 조회하고 현재 시즌은 API에서 자동으로 감지한다. API 클라이언트는 응답을 캐시하고 동시 요청 수를 제한하며 429 응답을 받으면 대기 후 재시도한다. 동시 접속자 수는 Steam Web API에서 가져온다.

## 실행

```bash
pip install -r requirements.txt

# .env 작성
printf 'BOT_TOKEN=...\nAPI_KEY=...\n' > .env

# 최초 실행은 슬래시 명령 동기화를 켜고 실행
SYNC_COMMANDS=1 python main.py

# 이후 실행
python main.py
```

## 설정

| 키 | 설명 |
|---|---|
| BOT_TOKEN | 디스코드 봇 토큰. 필수 |
| API_KEY | bser Open API 키. 필수 |
| STEAM_API_KEY | Steam Web API 키. 없으면 `/동접`이 동작하지 않는다 |
| SENTRY_DSN | Sentry DSN. 비워 두면 어디로도 전송하지 않는다 |
| FOOTER_ICON | 응답 푸터 아이콘 URL |
| SYNC_COMMANDS | `1`이면 시작 시 슬래시 명령을 동기화한다 |
